---
"cubic-loop": patch
---

README: document the `claude` CLI install path as an alternative to
the in-app `/plugin` commands. Same plugin (`cubic@cubic-loop`) and
same marketplace (`tgorka/cubic-loop`); the CLI form is handy for
pre-Claude-Code setup and for scripting machine provisioning. Adds
update / uninstall / list / scope examples and points readers at
`claude plugin --help` for the full surface.
