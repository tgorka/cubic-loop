## Description

<!-- What does this change and why? User-facing impact? -->

## Related Issues

<!-- Link any GitHub issues this PR addresses (e.g., "Closes #123"). -->

## Changesets

- [ ] Added a Changeset entry via `bun run changeset` describing this change (skip for docs-only or chore PRs).

## Quality Gates

- [ ] `bun run check` exits 0 locally (Biome lint + tests pass).
- [ ] `bunx tsc --noEmit` exits 0.
- [ ] Tests added or updated for any behavior change.

## Skill Changes

- [ ] If this PR modifies `skills/cubic-loop/SKILL.md` or any reference file, the change is intentional and the user-facing impact is captured in the Description above.
